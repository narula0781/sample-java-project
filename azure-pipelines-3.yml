# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

resources:
  pipelines:
  - pipeline: pipe1
    source: pipe1file
    trigger:
      branches:
      - master

stages:
- stage: stage1
  jobs:
      - job: test2
        displayName: test
        pool: 'Azure Pipeline 2'
        steps:
        - task: Powershell@2
          inputs:
              targetType: inline
              script: |
                Write-Host "Hello World!"